
// ["hello", "world", "hi", "bye"]

let longestWord = ["hello", "world", "hi", "bye"]

const longestWord = (arr) => {
  let lg = 0
  let longestWord
  for(let i=0; i < arr.length; i++){
    if(arr[i].length > long){
      lg = arr[i].length
      longestWord = arr[i]
    }
  }
  return longestWord
}

console.log("The longest word is: + longestWord(longestWordArr) )
